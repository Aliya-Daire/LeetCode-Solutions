**in C++**

class Solution {
public:
    int numTilings(int n) {
        // always 2 x n board, so 2 * n and subtract 1
        int ways_to_tile = (2 * n) - 1;
        return ways_to_tile; // return int
    }
};
